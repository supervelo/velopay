# Copyright 2021 - 2023 Transflox LLC. All rights reserved.

apiVersion: v1
kind: Service
metadata:
  name: velopay
  namespace: staging
  labels:
    app: gateway
spec:
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort
  selector:
    app: velopay
